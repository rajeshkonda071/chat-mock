# Chat Mock - Quick Start Guide

## 🚀 Running the Application (5 minutes)

### Step 1: Start the Backend
```bash
# Navigate to the server directory
cd server

# Install dependencies
npm install

# Start the server (runs on port 5000)
npm start
```

You should see: `Server running on http://localhost:5000`

### Step 2: Start the Frontend (New Terminal)
```bash
# Navigate to the client directory
cd client

# Install dependencies
npm install

# Start React app (opens at http://localhost:3000)
npm start
```

### ✅ You're Done!
The application is now running. Open `http://localhost:3000` in your browser.

---

## 📋 What You Can Do

1. **Start New Chat**: Click the "Start New Chat" button on the landing page
2. **Ask Questions**: Type any question and hit Send
3. **View Responses**: See answers with structured tables
4. **Give Feedback**: Click 👍 or 👎 on responses
5. **Toggle Theme**: Click the sun/moon icon to switch themes
6. **View History**: Click any session in the left sidebar
7. **Browse Sessions**: See all your chat sessions in the sidebar

---

## 🔌 API Base URL
- Backend: `http://localhost:5000`
- Frontend: `http://localhost:3000`

## 🛑 Stop the Application
- Press `Ctrl+C` in both terminals to stop the servers

---

## ❓ Need Help?
Refer to the main `README.md` file for detailed documentation.
